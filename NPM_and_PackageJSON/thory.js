// --------------------------------------------- NPM & package.json --------------------------------------------- //
// ChatGPT Se Liya Hua Syntex ---> NPM Kya Hai?
// NPM (Node Package Manager) duniya ka sabse bada software registry hai. Isme hazaro pre-built packages hote hain jo developers apne project me direct use kar sakte hain, taaki unhe code scratch se na likhna pade.

// --------------------------------------------- package.json kya hai? --------------------------------------------- //
// ChatGPT Se Liya Hua Syntex ---> package.json ka Use.
// package.json ek configuration file hoti hai jo hamare project ke saare metadata ko hold karti hai (jaise project ka naam, version, aur dependencies). 
// Jab hum 'npm init' run karte hain, tab ye file automatically generate hoti hai.

// --------------------------------------------- Dependencies vs DevDependencies --------------------------------------------- //
// ChatGPT Se Liya Hua Syntex ---> Dono me kya fark hai?
// 1. Dependencies: Wo packages jo production me code run karne ke liye zaroori hote hain (e.g., express, mongoose). Inhe install karne ke liye hum 'npm install <package-name>' use karte hain.
// 2. DevDependencies: Wo packages jo sirf development aur testing ke dauran use hote hain (e.g., nodemon, jest). Inhe production me deploy nahi kiya jata. Inhe install karne ke liye hum 'npm install -D <package-name>' use karte hain.

// --------------------------------------------- package-lock.json --------------------------------------------- //
// ChatGPT Se Liya Hua Syntex ---> package-lock.json.
// Ye file automatically ban jati hai jab hum NPM se koi package install karte hain. Ye make sure karti hai ki humare project me packages ke exact versions lock ho jayein, taaki doosre system pe code run karte waqt versioning ka koi issue na aaye.
